<%- include ('partials/header.ejs') %> 

<%- include ('partials/navbar.ejs') %> 

<main role="main">

    <!-- Principal jumbotron, para a principal mensagem de marketing ou call to action -->
    <div class="jumbotron">
      <div class="container">
        <h1 class="display-3">Olá, mundo!</h1>
        <p>Este é um template para um simples marketing ou website informacional. Ele inclui um callout grande (denominado jumbotron) e três conteúdos adicionais. Use-o como um ponto de partida para algo mais peculiar.</p>
        <p><a class="btn btn-primary btn-lg" href="#" role="button">Saiba mais »</a></p>
      </div>
    </div>

    <div class="container">

        <% articles.forEach(article => { %> 

            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Artigo: <%= article.title %></h5>
                    <hr my-2>
                    <span class="badge badge-pill badge-warning"> <%= article.category.title %></span>
                    <span class="badge badge-pill badge-secondary float-right">Criada em: <%= article.createdAt.toLocaleDateString("pt-BR", {hour: 'numeric', minute: 'numeric'}) %> horas %></span>
                    <p class="card-text">
                        <%- article.body.substring(0, 500) %> <a href="/<%= article.slug %>">(leia mais)</a>
                    </p>
                </div>
                <a href="/<%= article.slug %>" class="btn btn-primary btn-sm">Ler artigo</a> 
                <span class="badge badge-pill badge-secondary float-right">Última atualização: <%= article.updatedAt.toLocaleDateString("pt-BR", {hour: 'numeric', minute: 'numeric'}) %> horas</span>
            </div>

        <% }) %>

    </div> <!-- /container -->

</main>

<%- include ('partials/footer.ejs') %> 